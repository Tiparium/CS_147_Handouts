# Assembler/Simulator Testing

This folder contains minimal tests used to probe the assembler and simulator
behavior. These files are not homework solutions.

Use `run_tests.sh` to generate `probe_outputs.out` with sample output syntax.
