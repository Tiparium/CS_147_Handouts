#!/usr/bin/env bash
set -e

# Phase 2 autograders grade from precomputed reports and summaries only.
# The base image already has the required shell/Python tools, so no runtime
# package installation is needed here.
exit 0
